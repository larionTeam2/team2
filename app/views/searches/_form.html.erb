<%= form_with(model: search, local: true) do |form| %>
  <% if search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search.errors.count, "error") %> prohibited this search from being saved:</h2>

      <ul>
      <% search.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :keywords %>
    <%= form.text_field :keywords, id: :search_keywords %>
  </div>

  <div class="field">
    <%= form.label :new %>
    <%= form.text_field :new, id: :search_new %>
  </div>

  <div class="field">
    <%= form.label :show %>
    <%= form.text_field :show, id: :search_show %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
